{
	"extends": "../../../../common/build/build-common/tsconfig.node16.json",
	"exclude": ["src/test/**/*"],
	"compilerOptions": {
		// PropertyDDS relies on legacy class field semantics (useDefineForClassFields: false),
		// so pin the target below ES2022, where TypeScript would enable it by default.
		"target": "ES2021",
		"downlevelIteration": true,
		"noUnusedLocals": false,
		"outDir": "./lib",
		"rootDir": "./src",
		// Types for 'node' are required by 'msgpackr', even though we avoid the
		// node-specific functionality at runtime.
		"types": ["node"],
		"noUncheckedIndexedAccess": false,
		"exactOptionalPropertyTypes": false,
	},
	"include": ["src/**/*"],
}
