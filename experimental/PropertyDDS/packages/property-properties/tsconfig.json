{
	"extends": "../../../../common/build/build-common/ts-common-config.json",
	"exclude": ["src/test/**/*"],
	"compilerOptions": {
		"strict": false,
		"allowJs": true,
		"checkJs": false,
		"rootDir": "./src",
		"outDir": "./dist",
		"composite": true,
		// Updating to ES 2022 causes to failing tests.
		// Resolving these issues is not a priority right now.
		"target": "ES2021",
	},
	"include": ["src/**/*"],
}
