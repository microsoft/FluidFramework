{
	"description": "Scripts for building WASM projects in Fluid",
	"homepage": "https://fluidframework.com",
	"repository": {
		"type": "git",
		"url": "https://github.com/microsoft/FluidFramework.git",
		"directory": "wasm"
	},
	"license": "MIT",
	"author": "Microsoft and contributors",
	"scripts": {
		"build": "node ./wasm-pack-workspace.js --debug && pnpm i && pnpm run build:ts",
		"build:release": "node ./wasm-pack-workspace.js --release && pnpm i && pnpm run build:ts",
		"build:ts": "pnpm run -r build",
		"clean": "rimraf \"web-packages/*/bundler\" \"web-packages/*/nodejs\" \"target\" && pnpm run -r clean",
		"test": "cargo test && pnpm run -r test"
	},
	"devDependencies": {
		"rimraf": "^3.0.2",
		"toml": "^3.0.0",
		"wasm-opt": "1.4.0",
		"wasm-pack": "^0.11.1"
	}
}
