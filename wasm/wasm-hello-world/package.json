{
	"scripts": {
		"build": "pnpm run build:wasm && pnpm i && pnpm run cleanup && tsc --project \"tsconfig.json\" && pnpm run cleanup",
		"build:wasm": "wasm-pack build --target bundler --out-dir ../target-web/bundler ./rust_src && wasm-pack build --target nodejs --out-dir ../target-web/nodejs ./rust_src",
		"cleanup": "rimraf ./target-web/*/package.json && rimraf ./target-web/*/.gitignore"
	},
	"devDependencies": {
		"rimraf": "^3.0.2",
		"typescript": "^4.8.3"
	}
}
