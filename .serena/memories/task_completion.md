# Task Completion Checklist

1. Ensure code compiles: `pnpm build --filter @fluidframework/<pkg>`
2. Run tests: `pnpm test` in the package directory
3. Run lint: `pnpm lint` in the package directory
4. Format: `pnpm format`
5. Use conventional commits
