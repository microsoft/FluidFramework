# Iteration 0015: storage Instructions

Status: planned
Branch: `rust-service-iteration-0015-storage`
Iteration source commit: `ff7d736642c2711b44ce0507b2319c188ff46129`
Owner: GitHub Copilot subagent
Report: `rust-service/iterations/0015/phase-2/storage.md`
Required environment: pinned Rust toolchain; disposable roots allowed; no dependency, manifest, lockfile, persistence-format, or durability-policy changes

## Assignment

Independently verify inherited dispositions for `sea-content-addressed`, `sea-memory`, `sea-file`, and `sea-file-durable`. The hypothesis is that an adequate storage disposition may cite a broad law or adjacent test that can pass while the backend-owned mutation, recovery, or validation decision is broken. Challenge every inherited row; direct backend evidence falsifies each proposed gap.

## Ownership

Writable: the four named crate roots and this report. Read-only: all other paths and iteration `0014` evidence. Do not change dependencies, manifests, lockfiles, persistence formats, durability policy, or shared APIs. Remove temporary roots.

## Expected Evidence

Reassess every inherited row, name exact decisions and discriminating tests, update inventory rows, and implement at most two unrelated repair clusters. Preserve unsupported fault and power-loss claims. A no-change result requires exact evidence. No machine-readable output or temporary storage is retained.

## Validation

Guard checkout identity. Run focused tests after edits, then workspace format, strict Clippy, warning-denied rustdoc, and all-target/all-feature tests for all four crates, plus diff, lockfile, ownership, and temporary-artifact checks.

## Escalation and Stopping Conditions

Stop when all rows discriminate, after two unrelated repair clusters, or at format, durability, shared API, dependency, manifest, or unavailable fault-evidence boundaries. Record remaining risks and triggers.

## Reporting Requirements

Use the generated workstream report. Record failed approaches, substantial effort sinks, human interventions, undocumented workarounds, cross-workstream dependencies, and candidate reusable skills while work occurs.
