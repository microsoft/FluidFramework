{{<layout}}
    {{$content}}
        <div id="content"></div>
    {{/content}}

    {{$scripts}}
        <script src="/public{{#hfurl}}/scripts/dist/main.bundle.js{{/hfurl}}"></script>
        <script>
            var npm = "{{ npm }}";
            var orderer = "{{ orderer }}";
            var storage = "{{ storage }}";
            var token = "{{ token }}";

            main.load(orderer, storage, npm, token);
        </script>
    {{/scripts}}
{{/layout}}