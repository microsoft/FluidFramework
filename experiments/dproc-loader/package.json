{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.10.5"
  },
  "scripts": {
    "postinstall": "lerna bootstrap --hoist --nohoist tslint-microsoft-contrib",
    "tsc": "lerna run tsc --stream --scope @prague/process-loader --include-filtered-dependencies"
  }
}
