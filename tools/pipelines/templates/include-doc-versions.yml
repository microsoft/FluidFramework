# Note: to define previousVersions as a list of multiple branch names, use a comma to separate the names 
# e.g. lts,2.0,1.0 would be split using ',' as the separator 
variables:
  - name: currentVersion
    value: main
  - name: previousVersions
    value: lts
  
