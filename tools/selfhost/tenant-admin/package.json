{
	"name": "fluid-selfhost-tenant-admin",
	"version": "1.0.0",
	"private": true,
	"description": "Tenant lifecycle CLI for the self-hosted Fluid stack. Provisions gitrest snapshot storage and registers tenants with riddler, with ownership metadata and key-rotation support. Zero runtime dependencies so it can run inside the routerlicious container image.",
	"homepage": "https://fluidframework.com",
	"repository": {
		"type": "git",
		"url": "https://github.com/microsoft/FluidFramework.git",
		"directory": "tools/selfhost/tenant-admin"
	},
	"license": "MIT",
	"author": "Microsoft and contributors",
	"main": "src/tenantManager.js",
	"bin": {
		"tenant-admin": "./bin/tenant-admin.js"
	},
	"scripts": {
		"clean": "rimraf --glob nyc",
		"test": "node --test \"test/*.test.js\""
	},
	"devDependencies": {
		"rimraf": "^6.1.3"
	},
	"engines": {
		"node": ">=18.0.0"
	}
}
