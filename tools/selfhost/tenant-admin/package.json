{
	"name": "fluid-selfhost-tenant-admin",
	"version": "1.0.0",
	"private": true,
	"description": "Tenant lifecycle CLI for the self-hosted Fluid stack. Provisions gitrest snapshot storage and registers tenants with riddler, with ownership metadata and key-rotation support.",
	"homepage": "https://fluidframework.com",
	"repository": {
		"type": "git",
		"url": "https://github.com/microsoft/FluidFramework.git",
		"directory": "tools/selfhost/tenant-admin"
	},
	"license": "MIT",
	"author": "Microsoft and contributors",
	"main": "src/tenantManager.js",
	"bin": {
		"tenant-admin": "./bin/tenant-admin.js"
	},
	"scripts": {
		"build": "node scripts/build.js",
		"clean": "rimraf --glob nyc",
		"preinstall": "node ../../../scripts/only-pnpm.cjs",
		"test": "node --test \"test/*.test.js\""
	},
	"dependencies": {
		"@azure/identity": "4.13.2"
	},
	"devDependencies": {
		"esbuild": "0.28.2",
		"rimraf": "^6.1.3"
	},
	"engines": {
		"node": ">=22.0.0"
	}
}
