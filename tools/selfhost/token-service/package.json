{
	"name": "fluid-selfhost-token-service",
	"version": "1.0.0",
	"private": true,
	"description": "Reference token-minting service for the self-hosted Fluid stack. Authenticates end users with Microsoft Entra ID via App Service Easy Auth, then mints short-lived Fluid access tokens signed with the tenant key.",
	"homepage": "https://fluidframework.com",
	"repository": {
		"type": "git",
		"url": "https://github.com/microsoft/FluidFramework.git",
		"directory": "tools/selfhost/token-service"
	},
	"license": "MIT",
	"author": "Microsoft and contributors",
	"main": "src/functions/*.js",
	"scripts": {
		"clean": "rimraf --glob nyc",
		"start": "func start",
		"test": "node --test \"test/*.test.js\""
	},
	"dependencies": {
		"@azure/functions": "^4.5.0"
	},
	"devDependencies": {
		"rimraf": "^6.1.3"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
