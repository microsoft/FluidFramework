apiVersion: v1
kind: Service
metadata:
    name: "loadtestaks1-3b03"
    labels:
        app: "loadtestaks1-3b03"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "loadtestaks1-3b03"
