{
  "name": "augloop",
  "version": "0.0.1",
  "description": "Augmentation Loop Agent for Prague",
  "main": "dist/index.js",
  "scripts": {
    "build": "concurrently \"npm run tslint\" \"npm run tsc\"",
    "tsc": "tsc",
    "tslint": "tslint 'src/**/*.ts*'",
    "start": "docker-compose up",
    "start:debug": "docker-compose -f docker-compose.yml -f docker-compose.debug.yml up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@augloop/runtime-client": "1.4.14",
    "@augloop/schemas": "^1.4.14",
    "@prague/routerlicious": "^0.1.2773",
    "concurrently": "^3.5.1",
    "jsonwebtoken": "^8.2.1",
    "nconf": "^0.10.0",
    "winston": "^2.4.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^7.2.7",
    "@types/nconf": "0.0.37",
    "@types/winston": "^2.3.9",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
