{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "npx lerna run build --scope @chaincode/counter --stream"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
