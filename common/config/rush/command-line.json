{
    "$schema": "https://developer.microsoft.com/json-schemas/rush/v5/command-line.schema.json",
    "commands": [{
        "commandKind": "bulk",
        "name": "clean",
        "summary": "Recursively runs npm 'clean' script in all packages that define one",
        "description": "Use to remove build artifacts such as 'dist/' 'lib/' and *.tsbuildinfo",
        "enableParallelism": true,
        "ignoreMissingScript": true
    }]
  }
