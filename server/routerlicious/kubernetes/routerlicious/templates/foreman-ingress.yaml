apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "foreman.fullname" . }}
  labels:
    app: {{ template "routerlicious.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    component: "{{ .Values.foreman.name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingress.class }}
    ingress.kubernetes.io/ssl-redirect: "true"
    nginx.org/websocket-services: {{ template "foreman.fullname" . }}
    nginx.org/proxy-send-timeout: "7200s"
    nginx.org/proxy-read-timeout: "7200s"
    nginx.org/client-max-body-size: "100m"
    ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  tls:
  - hosts:
    - {{ .Values.foreman.host }}
    secretName: {{ .Values.foreman.cert }}
  rules:
  - host: {{ .Values.foreman.host }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ template "foreman.fullname" . }}
          servicePort: 80