version: '3'
services:
    gateway:
        build: .
        ports:
            - "3000:3000"
        command: node dist/www.js
        environment:
            - DEBUG=fluid:*
            - NODE_ENV=development
            - login__microsoft__clientId
            - login__microsoft__secret
            - login__accounts
        restart: always
