type Hero {
     id: Int!
     name: String!
}

type Query {
     heroes(id: Int): [Character!]!
}

type Mutation {
     renameHero(id: Int!, name: String!): Character!
}

type Subscription {
     herosUpdate: [Character!]!
     heroUpdate(id: Int): Character!
}
