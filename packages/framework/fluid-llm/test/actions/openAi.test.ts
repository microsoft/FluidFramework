// import { ChatGPTLLM } from "../../src/chatGptLlm";

describe("Summarize LLM Action", () => {
	// This test was just used to confirm dotenv is working as expected.
	test("Pulls api key from .env when undefined in constructor", () => {
		// const chatGptLLM = new ChatGPTLLM();
		// expect(chatGptLLM.apiKey).toBeDefined();
		// console.log(`apiKey ${chatGptLLM.apiKey}`);
	});
});
