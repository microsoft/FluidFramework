## Alpha API Report File for "@fluidframework/eval-app"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

/// <reference types="node" />

// @alpha @legacy
export function createEvalServer(config: EvalServerConfig): http.Server;

// @alpha @legacy
export interface EvalServerConfig {
    appName: string;
    checkAuth?: () => Promise<boolean>;
    customGenerationProperties?: Record<string, CustomPropertyValueType[]>;
    datasetsDir: string;
    defaultGeneratorModel: string;
    defaultJudgeModel: string;
    modelOptions: string[];
    port?: number;
    resultsDir: string;
    runAuth?: (progress: ProgressReporter) => Promise<void>;
    runGeneration: (request: RunGenerationRequest, progress: ProgressReporter, signal: AbortSignal) => Promise<RunGenerationResult>;
}

// @alpha @legacy
export interface ProgressReporter {
    progress(message: string): void;
    warn(message: string): void;
}

// @alpha @legacy
export interface RunGenerationRequest {
    customGenerationProperties?: Record<string, CustomPropertyValueType>;
    dataset: string;
    judgeModel: string;
    model: string;
}

// @alpha @legacy
export interface RunGenerationResult extends RunOptions {
    customEvaluators?: IEvaluator[];
    llmClient: ILLMClient;
    resultsDirPath: string;
}

// (No @packageDocumentation comment for this package)

```
