{
	"extends": "../../../common/build/build-common/tsconfig.node16.json",
	// Only the compatOptions module is required from a CJS build
	// for use in mocha configuration.
	"include": ["src/compatOptions.ts"],
	"compilerOptions": {
		"types": ["node"],
		"rootDir": "./src",
		"outDir": "./dist",
	},
}
