{
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"targets": {
		"build": {
			"executor": "nx:noop"
		},
		"build:commonjs": {
			"executor": "nx:noop"
		},
		"build:compile": {
			"executor": "nx:noop"
		},
		"tsc": {
			"dependsOn": ["^tsc", "^api", "build:genver", "ts2esm", "typetests:gen"]
		},
		"build:esnext": {
			"dependsOn": ["^tsc", "^api", "build:genver", "ts2esm", "^build:esnext", "typetests:gen"]
		},
		"check:exports": {
			"dependsOn": ["tsc", "build:esnext"]
		},
		"check:format": {
			"executor": "nx:noop"
		},
		"format": {
			"executor": "nx:noop"
		},
		"lint": {
			"executor": "nx:noop"
		},
		"lint:fix": {
			"executor": "nx:noop"
		},
		"test": {
			"executor": "nx:noop"
		}
	}
}
