import { MarkdownDocumenterConfig } from "./MarkdownDocumenterConfig";

/**
 * TODO
 */
export class MarkdownDocumenter {
    /**
     * TODO
     */
    private readonly config: MarkdownDocumenterConfig;
    public constructor(config: MarkdownDocumenterConfig) {
        this.config = config;
    }
}
