{
  "name": "@prague/prague-dump",
  "version": "0.10.0",
  "description": "",
  "main": "dist/pragueDump.js",
  "bin": {
    "prague-dump": "bin/prague-dump"
  },
  "scripts": {
    "build": "concurrently npm:build:compile npm:tslint",
    "build:compile": "tsc",
    "build:full": "npm run build",
    "build:full:compile": "npm run build:compile",
    "clean": "rimraf dist *.tsbuildinfo package-deps.json *.build.log",
    "tslint": "tslint --project tsconfig.json --format verbose"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@prague/odsp-socket-storage": "^0.10.0",
    "@prague/odsp-utils": "^0.10.0",
    "@prague/protocol-definitions": "^0.10.0",
    "@prague/routerlicious-socket-storage": "^0.10.0",
    "@prague/runtime-definitions": "^0.10.0",
    "@prague/utils": "^0.10.0"
  },
  "devDependencies": {
    "@prague/build-common": "^0.10.0",
    "@types/node": "^10.12.12",
    "concurrently": "^4.1.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "~3.4.5"
  }
}
