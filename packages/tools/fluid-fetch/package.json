{
  "name": "@microsoft/fluid-fetch",
  "version": "0.10.0",
  "description": "",
  "main": "dist/fluidFetch.js",
  "bin": {
    "fluid-fetch": "bin/fluid-fetch"
  },
  "scripts": {
    "build": "concurrently npm:build:compile npm:tslint",
    "build:compile": "tsc",
    "build:full": "npm run build",
    "build:full:compile": "npm run build:compile",
    "clean": "rimraf dist *.tsbuildinfo package-deps.json *.build.log",
    "tslint": "tslint --project tsconfig.json --format verbose"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@prague/odsp-socket-storage": "^0.10.0",
    "@microsoft/fluid-odsp-utils": "^0.10.0",
    "@prague/protocol-definitions": "^0.10.0",
    "@prague/routerlicious-socket-storage": "^0.10.0",
    "@microsoft/fluid-runtime-definitions": "^0.10.0",
    "@prague/utils": "^0.10.0"
  },
  "devDependencies": {
    "@microsoft/fluid-build-common": "^0.10.0",
    "@types/node": "^10.12.12",
    "concurrently": "^4.1.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "~3.4.5"
  }
}
