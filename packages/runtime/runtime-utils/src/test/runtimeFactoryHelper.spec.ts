/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */

import Sinon from "sinon";
import { strict as assert } from "assert";
import { RuntimeFactoryHelper } from "../runtimeFactoryHelper";

describe("RuntimeFactoryHelper", () => {
    it("Instantiate from existing when flag is set", () => {
    });
    it("Instantiate from existing when context is existing", () => {
    });
    it("Instantiate from existing when flag is set and context not existing", () => {
    });
});
