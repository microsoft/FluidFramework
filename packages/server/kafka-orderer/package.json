{
  "name": "@prague/kafka-orderer",
  "version": "0.4.0",
  "description": "Prague ordering services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "concurrently npm:build:compile npm:tslint",
    "build:compile": "npm run tsc",
    "build:full": "npm run build",
    "build:full:compile": "npm run build:compile",
    "clean": "rimraf dist lib *.tsbuildinfo package-deps.json *.build.log",
    "tsc": "tsc",
    "tslint": "tslint --project tsconfig.json --format verbose"
  },
  "author": "Microsoft",
  "repository": "microsoft/prague",
  "devDependencies": {
    "@prague/build-common": "^0.4.0",
    "concurrently": "^4.1.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "tslint-microsoft-contrib": "^6.0.0",
    "typescript": "~3.4.5"
  },
  "dependencies": {
    "@prague/container-definitions": "^0.4.0",
    "@prague/services-core": "^0.4.0",
    "@types/lodash": "^4.14.118",
    "@types/node": "^10.12.12",
    "lodash": "^4.17.11",
    "moniker": "^0.1.2"
  }
}
