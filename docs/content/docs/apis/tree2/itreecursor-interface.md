{
  "title": "ITreeCursor Interface",
  "summary": "A stateful low-level interface for reading tree data.",
  "kind": "Interface",
  "members": {
    "PropertySignature": {
      "[CursorMarker]": "/docs/apis/tree2/itreecursor-interface#_cursormarker_-propertysignature",
      "chunkLength": "/docs/apis/tree2/itreecursor-interface#chunklength-propertysignature",
      "chunkStart": "/docs/apis/tree2/itreecursor-interface#chunkstart-propertysignature",
      "fieldIndex": "/docs/apis/tree2/itreecursor-interface#fieldindex-propertysignature",
      "mode": "/docs/apis/tree2/itreecursor-interface#mode-propertysignature",
      "pending": "/docs/apis/tree2/itreecursor-interface#pending-propertysignature",
      "type": "/docs/apis/tree2/itreecursor-interface#type-propertysignature",
      "value": "/docs/apis/tree2/itreecursor-interface#value-propertysignature"
    },
    "MethodSignature": {
      "enterField": "/docs/apis/tree2/itreecursor-interface#enterfield-methodsignature",
      "enterNode": "/docs/apis/tree2/itreecursor-interface#enternode-methodsignature",
      "exitField": "/docs/apis/tree2/itreecursor-interface#exitfield-methodsignature",
      "exitNode": "/docs/apis/tree2/itreecursor-interface#exitnode-methodsignature",
      "firstField": "/docs/apis/tree2/itreecursor-interface#firstfield-methodsignature",
      "firstNode": "/docs/apis/tree2/itreecursor-interface#firstnode-methodsignature",
      "getFieldKey": "/docs/apis/tree2/itreecursor-interface#getfieldkey-methodsignature",
      "getFieldLength": "/docs/apis/tree2/itreecursor-interface#getfieldlength-methodsignature",
      "getFieldPath": "/docs/apis/tree2/itreecursor-interface#getfieldpath-methodsignature",
      "getPath": "/docs/apis/tree2/itreecursor-interface#getpath-methodsignature",
      "nextField": "/docs/apis/tree2/itreecursor-interface#nextfield-methodsignature",
      "nextNode": "/docs/apis/tree2/itreecursor-interface#nextnode-methodsignature",
      "seekNodes": "/docs/apis/tree2/itreecursor-interface#seeknodes-methodsignature",
      "skipPendingFields": "/docs/apis/tree2/itreecursor-interface#skippendingfields-methodsignature"
    }
  },
  "package": "@fluid-experimental/tree2",
  "unscopedPackageName": "tree2"
}

[//]: # (Do not edit this file. It is automatically generated by @fluidtools/api-markdown-documenter.)

A stateful low-level interface for reading tree data.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

## Signature {#itreecursor-signature}

```typescript
export interface ITreeCursor
```

## Remarks {#itreecursor-remarks}

Cursor exists so that specialized data formats can be viewed through a common abstraction. This allows performance optimizations to be done based on data.

A tree cursor is similar to a database cursor in that it allows for the efficient traversal over the contents of a tree. Note that unlike a database cursor, tree cursors may be invalidated after any edit to the tree. For a cursor-like structure that also remains valid across edits, see [AnchorNode](/docs/apis/tree2/anchornode-interface).

## Properties

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Property
      </th>
      <th>
        Alerts
      </th>
      <th>
        Modifiers
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#_cursormarker_-propertysignature'>[CursorMarker]</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span>true</span>
      </td>
      <td>
        Marks this object as a cursor.
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#chunklength-propertysignature'>chunkLength</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
        <p>
          Length of current chunk. Since an entire chunk always has the same <code>pending</code> value, can be used to help skip over all of a pending chunk at once.
        </p>
        <p>
          TODO: Add optional APIs to access underlying chunks so readers can accelerate processing of chunk formats they understand.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#chunkstart-propertysignature'>chunkStart</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
        <p>
          Index (within its parent field) of the first node in the current chunk. Always less than or equal to <code>currentIndexInField</code>.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#fieldindex-propertysignature'>fieldIndex</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
        <p>
          Index (within its parent field) of the current node.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#mode-propertysignature'>mode</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2/cursorlocationtype-enum'>CursorLocationType</a></span>
      </td>
      <td>
        What kind of place the cursor is at. Determines which operations are allowed.
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#pending-propertysignature'>pending</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#type-propertysignature'>type</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2#treetype-typealias'>TreeType</a></span>
      </td>
      <td>
        <p>
          The type of the currently selected node.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>, and not <code>pending</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#value-propertysignature'>value</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <code>readonly</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2#value-typealias'>Value</a></span>
      </td>
      <td>
        <p>
          The value associated with the currently selected node.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>, and not <code>pending</code>.
        </p>
      </td>
    </tr>
  </tbody>
</table>

## Methods

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Method
      </th>
      <th>
        Alerts
      </th>
      <th>
        Return Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#enterfield-methodsignature'>enterField(key)</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>void</span>
      </td>
      <td>
        <p>
          Navigate to the field with the specified <code>key</code> and set the mode to <code>Fields</code>.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>, and not <code>pending</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#enternode-methodsignature'>enterNode(childIndex)</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>void</span>
      </td>
      <td>
        <p>
          Sets current node to the node at the provided <code>index</code> of the current field.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Fields</code>, and not <code>pending</code>. Sets mode to <code>Nodes</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#exitfield-methodsignature'>exitField()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>void</span>
      </td>
      <td>
        <p>
          Navigate up to parent node. Sets mode to <code>Nodes</code>
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Fields</code>.
        </p>
        <p>
          TODO: what to do if at root?
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#exitnode-methodsignature'>exitNode()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>void</span>
      </td>
      <td>
        <p>
          Navigate up to parent field. Sets mode to <code>Fields</code>
        </p>
        <p>
          Same as seek Number.POSITIVE_INFINITY, but only valid when <code>mode</code> is <code>Nodes</code>.
        </p>
        <p>
          TODO: what to do if at root? TODO: Maybe merge with upToNode to make a single &quot;Up&quot;?
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#firstfield-methodsignature'>firstField()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        <p>
          Enters the first non-empty field (setting mode to <code>Fields</code>) so fields can be iterated with <code>nextField</code> and <code>skipPendingFields</code>.
        </p>
        <p>
          If there are no fields, mode is returned to <code>Nodes</code> and false is returned.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Nodes</code> and not <code>pending</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#firstnode-methodsignature'>firstNode()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        <p>
          Moves to the first node of the selected field, setting mode to <code>Nodes</code>.
        </p>
        <p>
          If field is empty, returns false instead.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Fields</code>, and not <code>pending</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#getfieldkey-methodsignature'>getFieldKey()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2#fieldkey-typealias'>FieldKey</a></span>
      </td>
      <td>
        <p>
          Returns the FieldKey for the current field.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Fields</code>, and not <code>pending</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#getfieldlength-methodsignature'>getFieldLength()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#getfieldpath-methodsignature'>getFieldPath(prefix)</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2/fielduppath-interface'>FieldUpPath</a></span>
      </td>
      <td>
        <p>
          Returns a path to the current field. See <a href='/docs/apis/tree2/fielduppath-interface'>FieldUpPath</a>.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Fields</code>.
        </p>
        <p>
          If no prefix is provided, assumes this cursor is treated as if it has a root node where its field keys are actually detached sequences. If the cursor is not rooted at such a node, the <code>prefix</code> should be used to ensure the path has the correct root. This requirement exists because <a href='/docs/apis/tree2/fielduppath-interface'>FieldUpPath</a>s are absolute paths and thus must be rooted in a detached sequence.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#getpath-methodsignature'>getPath(prefix)</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span><a href='/docs/apis/tree2/uppath-interface'>UpPath</a> &#124; undefined</span>
      </td>
      <td>
        <p>
          Returns a path to the current node. See <a href='/docs/apis/tree2/uppath-interface'>UpPath</a>.
        </p>
        <p>
          Only valid when <code>mode</code> is <code>Nodes</code>.
        </p>
        <p>
          If no prefix is provided, assumes this cursor is treated as if it has a root node where its field keys are actually detached sequences. If the cursor is not rooted at such a node, the <code>prefix</code> should be used to ensure the path has the correct root. This requirement exists because <a href='/docs/apis/tree2/uppath-interface'>UpPath</a>s are absolute paths and thus must be rooted in a detached sequence.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#nextfield-methodsignature'>nextField()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        <p>
          Moves the &quot;current field&quot; forward one in an arbitrary field traversal order, skipping any empty fields.
        </p>
        <p>
          If there is no remaining field to iterate to, returns false and navigates up to the parent setting the mode to <code>Nodes</code>.
        </p>
        <p>
          Order of fields is only guaranteed to be consistent through a single iteration.
        </p>
        <p>
          If skipPending, skip past fields which are currently pending. This can be used to skip to the end of a large number of consecutive pending fields.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Fields</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#nextnode-methodsignature'>nextNode()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        The same as <code>seekNodes(1)</code>, but might be faster.
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#seeknodes-methodsignature'>seekNodes(offset)</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        <p>
          Moves <code>offset</code> nodes in the field. If seeking to exactly past either end, returns false and navigates up to the parent field (setting mode to <code>Fields</code>).
        </p>
        <p>
          Allowed if mode is <code>Nodes</code>.
        </p>
      </td>
    </tr>
    <tr>
      <td>
        <a href='/docs/apis/tree2/itreecursor-interface#skippendingfields-methodsignature'>skipPendingFields()</a>
      </td>
      <td>
        <code>ALPHA</code>
      </td>
      <td>
        <span>boolean</span>
      </td>
      <td>
        <p>
          Moves the &quot;current field&quot; forward until <code>pending</code> is <code>false</code>.
        </p>
        <p>
          If there are no remaining field to iterate to, returns false and navigates up to the parent setting the mode to <code>Nodes</code>.
        </p>
        <p>
          Order of fields is only guaranteed to be consistent through a single iteration.
        </p>
        <p>
          Allowed when <code>mode</code> is <code>Fields</code>.
        </p>
      </td>
    </tr>
  </tbody>
</table>

## Property Details

### \[CursorMarker\] (ALPHA) {#_cursormarker_-propertysignature}

Marks this object as a cursor.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#_cursormarker_-signature}

```typescript
readonly [CursorMarker]: true;
```

### chunkLength (ALPHA) {#chunklength-propertysignature}

Length of current chunk. Since an entire chunk always has the same `pending` value, can be used to help skip over all of a pending chunk at once.

TODO: Add optional APIs to access underlying chunks so readers can accelerate processing of chunk formats they understand.

Only valid when `mode` is `Nodes`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#chunklength-signature}

```typescript
readonly chunkLength: number;
```

### chunkStart (ALPHA) {#chunkstart-propertysignature}

Index (within its parent field) of the first node in the current chunk. Always less than or equal to `currentIndexInField`.

Only valid when `mode` is `Nodes`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#chunkstart-signature}

```typescript
readonly chunkStart: number;
```

### fieldIndex (ALPHA) {#fieldindex-propertysignature}

Index (within its parent field) of the current node.

Only valid when `mode` is `Nodes`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#fieldindex-signature}

```typescript
readonly fieldIndex: number;
```

### mode (ALPHA) {#mode-propertysignature}

What kind of place the cursor is at. Determines which operations are allowed.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#mode-signature}

```typescript
readonly mode: CursorLocationType;
```

#### Remarks {#mode-remarks}

Users of cursors frequently need to refer to places in trees, both fields and nodes. Approaches other than having the cursor have separate modes for these cases had issues even worse than having the two modes.

For example, modeling fields as parent + key has issues when there is no parent, and doesn't provide a great way to do iteration over fields while also having a nice API and making it easy for the implementation to track state (like its current location inside a sequence tree of fields) while traversing without having to allocate some state management for that.

Another approach, of using arrays of cursors for fields (like we currently do for inserting content) is very inefficient and better addressed by a dual mode cursor.

Another approach, of using the first node in a field when referring to the field gets confusing since it's unclear if a given cursor means that node, or that node, and the ones after it, and in the second case, it's hard to restore the cursor back to the right state when returning. It also doesn't work for empty fields. Overall there just didn't seem to be a way that sucked less than the dual mode API.

### pending (ALPHA) {#pending-propertysignature}

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#pending-signature}

```typescript
readonly pending: boolean;
```

### type (ALPHA) {#type-propertysignature}

The type of the currently selected node.

Only valid when `mode` is `Nodes`, and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#type-signature}

```typescript
readonly type: TreeType;
```

### value (ALPHA) {#value-propertysignature}

The value associated with the currently selected node.

Only valid when `mode` is `Nodes`, and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#value-signature}

```typescript
readonly value: Value;
```

## Method Details

### enterField (ALPHA) {#enterfield-methodsignature}

Navigate to the field with the specified `key` and set the mode to `Fields`.

Only valid when `mode` is `Nodes`, and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#enterfield-signature}

```typescript
enterField(key: FieldKey): void;
```

#### Parameters {#enterfield-parameters}

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Parameter
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        key
      </td>
      <td>
        <span><a href='/docs/apis/tree2#fieldkey-typealias'>FieldKey</a></span>
      </td>
      <td>
      </td>
    </tr>
  </tbody>
</table>

### enterNode (ALPHA) {#enternode-methodsignature}

Sets current node to the node at the provided `index` of the current field.

Allowed when `mode` is `Fields`, and not `pending`. Sets mode to `Nodes`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#enternode-signature}

```typescript
enterNode(childIndex: number): void;
```

#### Parameters {#enternode-parameters}

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Parameter
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        childIndex
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
      </td>
    </tr>
  </tbody>
</table>

### exitField (ALPHA) {#exitfield-methodsignature}

Navigate up to parent node. Sets mode to `Nodes`

Only valid when `mode` is `Fields`.

TODO: what to do if at root?

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#exitfield-signature}

```typescript
exitField(): void;
```

### exitNode (ALPHA) {#exitnode-methodsignature}

Navigate up to parent field. Sets mode to `Fields`

Same as seek Number.POSITIVE\_INFINITY, but only valid when `mode` is `Nodes`.

TODO: what to do if at root? TODO: Maybe merge with upToNode to make a single "Up"?

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#exitnode-signature}

```typescript
exitNode(): void;
```

### firstField (ALPHA) {#firstfield-methodsignature}

Enters the first non-empty field (setting mode to `Fields`) so fields can be iterated with `nextField` and `skipPendingFields`.

If there are no fields, mode is returned to `Nodes` and false is returned.

Allowed when `mode` is `Nodes` and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#firstfield-signature}

```typescript
firstField(): boolean;
```

#### Returns {#firstfield-returns}

**Return type:** boolean

### firstNode (ALPHA) {#firstnode-methodsignature}

Moves to the first node of the selected field, setting mode to `Nodes`.

If field is empty, returns false instead.

Allowed when `mode` is `Fields`, and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#firstnode-signature}

```typescript
firstNode(): boolean;
```

#### Returns {#firstnode-returns}

**Return type:** boolean

### getFieldKey (ALPHA) {#getfieldkey-methodsignature}

Returns the FieldKey for the current field.

Allowed when `mode` is `Fields`, and not `pending`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#getfieldkey-signature}

```typescript
getFieldKey(): FieldKey;
```

#### Returns {#getfieldkey-returns}

**Return type:** [FieldKey](/docs/apis/tree2#fieldkey-typealias)

### getFieldLength (ALPHA) {#getfieldlength-methodsignature}

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#getfieldlength-signature}

```typescript
getFieldLength(): number;
```

#### Returns {#getfieldlength-returns}

the number of immediate children in the current field.

Allowed when `mode` is `Fields`, and not `pending`.

**Return type:** number

### getFieldPath (ALPHA) {#getfieldpath-methodsignature}

Returns a path to the current field. See [FieldUpPath](/docs/apis/tree2/fielduppath-interface).

Only valid when `mode` is `Fields`.

If no prefix is provided, assumes this cursor is treated as if it has a root node where its field keys are actually detached sequences. If the cursor is not rooted at such a node, the `prefix` should be used to ensure the path has the correct root. This requirement exists because [FieldUpPath](/docs/apis/tree2/fielduppath-interface)s are absolute paths and thus must be rooted in a detached sequence.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#getfieldpath-signature}

```typescript
getFieldPath(prefix?: PathRootPrefix): FieldUpPath;
```

#### Parameters {#getfieldpath-parameters}

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Parameter
      </th>
      <th>
        Modifiers
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        prefix
      </td>
      <td>
        optional
      </td>
      <td>
        <span><a href='/docs/apis/tree2/pathrootprefix-interface'>PathRootPrefix</a></span>
      </td>
      <td>
        optional overrides to apply to the root of the returned path. See <a href='/docs/apis/tree2/pathrootprefix-interface'>PathRootPrefix</a>. This adjusts the path as if the tree data accessible to this cursor is part of a larger tree.
      </td>
    </tr>
  </tbody>
</table>

#### Returns {#getfieldpath-returns}

a path to the current field.

**Return type:** [FieldUpPath](/docs/apis/tree2/fielduppath-interface)

### getPath (ALPHA) {#getpath-methodsignature}

Returns a path to the current node. See [UpPath](/docs/apis/tree2/uppath-interface).

Only valid when `mode` is `Nodes`.

If no prefix is provided, assumes this cursor is treated as if it has a root node where its field keys are actually detached sequences. If the cursor is not rooted at such a node, the `prefix` should be used to ensure the path has the correct root. This requirement exists because [UpPath](/docs/apis/tree2/uppath-interface)s are absolute paths and thus must be rooted in a detached sequence.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#getpath-signature}

```typescript
getPath(prefix?: PathRootPrefix): UpPath | undefined;
```

#### Parameters {#getpath-parameters}

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Parameter
      </th>
      <th>
        Modifiers
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        prefix
      </td>
      <td>
        optional
      </td>
      <td>
        <span><a href='/docs/apis/tree2/pathrootprefix-interface'>PathRootPrefix</a></span>
      </td>
      <td>
        optional overrides to apply to the root of the returned path. See <a href='/docs/apis/tree2/pathrootprefix-interface'>PathRootPrefix</a>. This adjusts the path as if the tree data accessible to this cursor is part of a larger tree.
      </td>
    </tr>
  </tbody>
</table>

#### Returns {#getpath-returns}

a path to the current node.

**Return type:** [UpPath](/docs/apis/tree2/uppath-interface) \| undefined

### nextField (ALPHA) {#nextfield-methodsignature}

Moves the "current field" forward one in an arbitrary field traversal order, skipping any empty fields.

If there is no remaining field to iterate to, returns false and navigates up to the parent setting the mode to `Nodes`.

Order of fields is only guaranteed to be consistent through a single iteration.

If skipPending, skip past fields which are currently pending. This can be used to skip to the end of a large number of consecutive pending fields.

Allowed when `mode` is `Fields`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#nextfield-signature}

```typescript
nextField(): boolean;
```

#### Returns {#nextfield-returns}

**Return type:** boolean

### nextNode (ALPHA) {#nextnode-methodsignature}

The same as `seekNodes(1)`, but might be faster.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#nextnode-signature}

```typescript
nextNode(): boolean;
```

#### Returns {#nextnode-returns}

**Return type:** boolean

### seekNodes (ALPHA) {#seeknodes-methodsignature}

Moves `offset` nodes in the field. If seeking to exactly past either end, returns false and navigates up to the parent field (setting mode to `Fields`).

Allowed if mode is `Nodes`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#seeknodes-signature}

```typescript
seekNodes(offset: number): boolean;
```

#### Parameters {#seeknodes-parameters}

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>
        Parameter
      </th>
      <th>
        Type
      </th>
      <th>
        Description
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        offset
      </td>
      <td>
        <span>number</span>
      </td>
      <td>
      </td>
    </tr>
  </tbody>
</table>

#### Returns {#seeknodes-returns}

**Return type:** boolean

### skipPendingFields (ALPHA) {#skippendingfields-methodsignature}

Moves the "current field" forward until `pending` is `false`.

If there are no remaining field to iterate to, returns false and navigates up to the parent setting the mode to `Nodes`.

Order of fields is only guaranteed to be consistent through a single iteration.

Allowed when `mode` is `Fields`.

WARNING: This API is provided as an alpha preview and may change without notice. Use at your own risk.

#### Signature {#skippendingfields-signature}

```typescript
skipPendingFields(): boolean;
```

#### Returns {#skippendingfields-returns}

**Return type:** boolean
