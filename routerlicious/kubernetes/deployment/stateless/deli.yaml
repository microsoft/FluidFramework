apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: deli
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: deli
    spec:
      containers:
      - name: prague
        image: prague.azurecr.io/prague:576
        command:
          - 'npm'
          - 'run'
          - 'deli'
        env:
        - name: DEBUG
          value: "routerlicious:*"
        - name: NODE_ENV
          value: production
        volumeMounts:
        - name: config
          mountPath: /usr/src/server/config.json
          subPath: config.json
      imagePullSecrets:
      - name: regsecret
      volumes:
        - name: config
          configMap:
            name: prague
