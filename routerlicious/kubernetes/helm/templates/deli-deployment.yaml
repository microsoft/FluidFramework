apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{ template "routerlicious.deli.fullname" . }}
  labels:
    app: {{ template "routerlicious.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    component: "{{ .Values.deli.name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.deli.replicas }}
  template:
    metadata:
      labels:
        app: {{ template "routerlicious.name" . }}
        component: "{{ .Values.deli.name }}"
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ template "routerlicious.deli.fullname" . }}
        image: "{{ .Values.image }}"
        imagePullPolicy: {{ default "" .Values.imagePullPolicy | quote }}
        command:
          - 'node'
          - 'dist/deli/index.js'
        env:
        - name: DEBUG
          value: "routerlicious:*"
        - name: NODE_ENV
          value: production
        volumeMounts:
        - name: config
          mountPath: /usr/src/server/config.json
          subPath: config.json
      imagePullSecrets:
      - name: regsecret
      volumes:
        - name: config
          configMap:
            name: prague
