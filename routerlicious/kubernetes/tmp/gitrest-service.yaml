apiVersion: v1
kind: Service
metadata:
  name: gitrest
  labels:
    app: gitrest
spec:
  ports:
    - name: ssh
      port: 22
      targetPort: ssh
    - name: http
      port: 80
      targetPort: http
  type: LoadBalancer
  selector:
    app: gitrest