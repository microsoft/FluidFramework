{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream --include-filtered-dependencies --scope @prague/deli",
    "tsc": "lerna run tsc --stream --include-filtered-dependencies --scope @prague/deli",
    "postinstall": "lerna bootstrap --hoist"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
