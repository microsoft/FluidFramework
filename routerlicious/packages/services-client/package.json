{
  "name": "@prague/services-client",
  "version": "0.1.0",
  "description": "Isomorphic services for communicating with Prague",
  "main": "dist/index.js",
  "module": "lib/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": "false",
  "scripts": {
    "build": "npm run tsc && npm run build:esnext && npm run tslint",
    "build:full": "npm run build",
    "tsc": "tsc",
    "build:esnext": "tsc --outDir lib --m esnext",
    "tslint": "tslint 'src/**/*.ts'"
  },
  "author": "Microsoft",
  "repository": "microsoft/prague",
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "@prague/gitresources": "^0.1.0",
    "@prague/runtime-definitions": "^0.1.0",
    "@types/node": "^10.5.7",
    "axios": "^0.18.0",
    "debug": "^3.1.0"
  }
}
