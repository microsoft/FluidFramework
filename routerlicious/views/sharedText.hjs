{{<layout}}
    {{$styles}}
        <link href="{{#hfurl}}/public/stylesheets/dist/style.css{{/hfurl}}" rel="stylesheet" />
    {{/styles}}

    {{$content}}
    {{/content}}

    {{$scripts}}
        <script src="{{#hfurl}}/public/scripts/dist/controller.min.js{{/hfurl}}"></script>
        <script>
            var config = {{{ config }}};
            var version = {{{ version }}};
            var options = {{{ options }}};
            var connect = {{{ connect }}};
            var token = "{{ token }}";
            console.log(`Starting load at - ${performance.now()}`);
            prague_controller.sharedText.load(
                "{{ documentId }}",
                version,
                token,
                {{ pageInk }},
                {{ disableCache }},
                config,
                "{{ template }}",
                connect,
                options);
        </script>
    {{/scripts}}
{{/layout}}