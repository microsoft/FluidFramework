import * as assert from "assert";

describe("Historian", () => {
    describe("Routes", () => {
        describe("Repository", () => {
            describe("Commits", () => {
                it("Can list recent commits", async () => {
                    assert(true);
                });
            });
        });
    });
});
