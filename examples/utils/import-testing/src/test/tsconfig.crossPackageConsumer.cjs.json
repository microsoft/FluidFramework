{
	// This config must be used in a "type": "commonjs" environment. (Use fluid-tsc commonjs.)
	"extends": "./tsconfig.crossPackageConsumer.json",
	// bundler is only produced and tested for ESM, so exclude it from CJS compilation.
	"exclude": ["./crossPackage/bundlerImporter.ts"],
	"compilerOptions": {
		"outDir": "../../dist/test",
	},
	"references": [
		{
			"path": "../../tsconfig.crossPackage.node16.cjs.json",
		},
	],
}
