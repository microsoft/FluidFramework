{
	// This config must be used in a "type": "commonjs" environment. (Use fluid-tsc commonjs.)
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"outDir": "../../dist/test",
	},
	// Cross-package importers are ESM-only compile-time tests (no CJS schema output exists).
	"exclude": ["./crossPackageImporter.ts", "./crossPackageBundlerImporter.ts"],
	"references": [
		{
			"path": "../../tsconfig.cjs.json",
		},
	],
}
