{
	// This config must be used in a "type": "commonjs" environment. (Use fluid-tsc commonjs.)
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"outDir": "./dist",
	},
	// Include crossPackageSchemaExports.ts which is excluded from the ESM build for isolation.
	"exclude": ["src/test/**/*"],
}
