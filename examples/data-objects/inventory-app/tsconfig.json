{
	"extends": "../../../common/build/build-common/tsconfig.node16.json",
	"compilerOptions": {
		"outDir": "./lib",
		"types": [
			"react",
			"react-dom",
			// TODO: its a bit concerning to have all these test specific things in here where they impact non test code
			//  and also for them to not actually work at making functioning intellisense in the tests.
			"jest",
			"puppeteer",
			"jest-environment-puppeteer",
			"expect-puppeteer",
		],
		// For some cases (like type assertions and some type related testing) its nice to have this off, and there isn't a separate config for tests.
		"noUnusedLocals": false,
	},
	"include": ["src/**/*"],
}
