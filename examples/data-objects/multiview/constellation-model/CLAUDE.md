# @fluid-example/multiview-constellation-model

Constellation model for the multiview sample demonstrating model-view separation.

## Build

- `pnpm build` - Build the package
- `pnpm build:esnext` - TypeScript compilation only
- `pnpm lint` - Run linting
- `pnpm lint:fix` - Fix lint issues

## Notes

- This is an example package (private, not published)
- Part of the multiview example demonstrating separation of model and view
- Depends on `@fluid-example/multiview-coordinate-interface` and `@fluid-example/multiview-coordinate-model`
- Uses `@fluidframework/map` for data storage
- Library-only package (no runnable app)
