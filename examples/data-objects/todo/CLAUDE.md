# @fluid-example/todo

Simple collaborative todo canvas application.

## Build

- `pnpm build` - Build the package
- `pnpm build:esnext` - TypeScript compilation only
- `pnpm lint` - Run linting
- `pnpm lint:fix` - Fix lint issues

## Run

- `pnpm start` - Start dev server (local mode)
- `pnpm start:tinylicious` - Start with Tinylicious server

## Test

- `pnpm test` - Run Jest tests (Puppeteer-based)

## Notes

- This is an example package (private, not published)
- Uses `@fluidframework/tree` (SharedTree), `@fluidframework/map`, and `@fluidframework/sequence`
- Uses `@fluidframework/react` for React bindings
- Good example of combining multiple DDSes in one application
