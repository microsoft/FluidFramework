---
"@fluidframework/react": minor
"__section": feature
---

React 19 support is added

`@fluidframework/react` now supports both React 18 and React 19.
Its implementation and test suite are checked against both versions.
Some `@alpha` TypeScript types have changed to remain compatible across both React versions, but runtime behavior is unchanged.
