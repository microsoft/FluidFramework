---
"@fluidframework/local-driver": minor
"__section": feature
---
Add a session-storage-backed ServiceClient

The new alpha `startSessionService` API provides a `ServiceClient` compatible way to use the browser-local Fluid service that retains attached documents across page reloads in the same browser tab.
