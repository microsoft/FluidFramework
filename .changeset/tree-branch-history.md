---
"fluid-framework": minor
"@fluidframework/tree": minor
"__section": tree
---
TreeBranchAlpha.history

Adds a new `history` property to `TreeBranchAlpha`, exposing a `TreeBranchHistory` object.
`TreeBranchHistory.size` can be used to query the number of commits currently retained in a branch's history.
